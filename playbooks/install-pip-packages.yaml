---
- hosts: localhost
  connection: local
  vars:
    packages:
      - visidata
      - thefuck
      - python-lsp-server[all]
      - pyright
      - autopep8
      - jupytext
      - jupyterlab
      - dbx
  tasks:
    - name: Installing python packages
      loop: "{{ packages }}"
      ansible.builtin.shell: "pip install {{ item }}"

