---
- hosts: localhost
  connection: local
  vars:
    packages:
      - bat
      - zoxide
      - ripgrep
      - stylua
  tasks:
    - name: Install cargo packages
      loop: "{{ packages }}"
      community.general.cargo: "name={{ item }}"
