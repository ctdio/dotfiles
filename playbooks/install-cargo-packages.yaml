---
- hosts: localhost
  connection: local
  vars:
    packages:
      - bat
      - zoxide
      - ripgrep
      - fd-find
      - stylua
      - xsv
  tasks:
    - name: Install cargo packages
      loop: "{{ packages }}"
      community.general.cargo:
        name: "{{ item }}"
        state: latest
