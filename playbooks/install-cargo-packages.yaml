---
- hosts: 127.0.0.1
  vars:
    packages:
      - bat
      - zoxide
      - ripgrep
  connection: local
  tasks:
    - name: Install cargo packages
      loop: "{{ packages }}"
      community.general.cargo: "name={{ item }}"
